server.modules += ( "mod_proxy" )

$HTTP["url"] =~ "^/(.*)$" {
  proxy.server = ( "" => ( ( "host" => "", "port" => 8080 ) ) )
  proxy.header = (
    "map-url" => (
      "^/(.*)$" => "/$1"
    )
  )
}
